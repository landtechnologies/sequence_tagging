service:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: planning-ml.k8-staging.landinsight.io
    service.beta.kubernetes.io/aws-load-balancer-extra-security-groups: "sg-73a11e0f"

resources:
  requests:
    memory: "1000Mi"
    cpu: "400m"

podDisruptionBudget: |
  minAvailable: 1

minPodReplicaCount: 2